name: shellcheck

on:
  push:
    branches: [main]
  pull_request:

env:
  # Pin shellcheck to the exact version used locally so CI and dev never drift
  # (apt's version floats and once flagged SC2015 that newer shellcheck ignores).
  # To bump: change the version, update your local install to match, and refresh
  # SHELLCHECK_SHA256 with the sha256 of the new linux.x86_64 tarball.
  SHELLCHECK_VERSION: v0.11.0
  SHELLCHECK_SHA256: 8c3be12b05d5c177a04c29e3c78ce89ac86f1595681cab149b65b97c4e227198

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Install pinned shellcheck
        run: |
          curl -fsSL -o shellcheck.tar.xz \
            "https://github.com/koalaman/shellcheck/releases/download/${SHELLCHECK_VERSION}/shellcheck-${SHELLCHECK_VERSION}.linux.x86_64.tar.xz"
          echo "${SHELLCHECK_SHA256}  shellcheck.tar.xz" | sha256sum -c -
          tar -xJf shellcheck.tar.xz
          sudo install -m 0755 "shellcheck-${SHELLCHECK_VERSION}/shellcheck" /usr/local/bin/shellcheck
          shellcheck --version
      - name: Run shellcheck
        # T8 п.5: перечисление файлов заменено на выборку — жёсткий список сам становится
        # местом, где забудут (ровно так и вышло: половина tests/*.sh в него не входила и
        # накопила замечания, а десятки директив `# shellcheck disable=` ни разу не
        # проверялись линтером). Теперь берём ВСЕ *.sh репозитория + tests/run (он без
        # расширения, но это bash) — новый .sh попадает под линт автоматически.
        #
        # М7 (ревью T8): extensionless bin/* ДОБАВЛЕН — отдельным прогоном на `-S warning`.
        # На `-S error` он давал 0 замечаний (то есть ничего не ловил), на `-S warning` — 7,
        # и среди них нашлись два мёртвых флага: `--project` в adapters/asana-comments и
        # `--subject` в bin/claude-auto-request. Уровень `warning`, а не дефолтный
        # style/info, — сознательно: у этих файлов есть предсуществующий info-шум
        # (SC2012 `ls | head`, SC2015), разбирать который — отдельная задача. Namespace
        # выбирается ПО SHEBANG, а не по имени: в bin/ лежат и node-скрипты
        # (claude-auto-liveness, dept-ledger, dept-inbox, dept-dispatcher, dept-rebase-check,
        # dept-memory-freshness), их shellcheck разбирать не должен.
        # channels/*/adapters/* сюда НЕ входит: там свой предсуществующий шум, отдельно.
        run: |
          mapfile -t sh_files < <(git ls-files '*.sh')
          shellcheck "${sh_files[@]}" tests/run
          mapfile -t bin_files < <(
            git ls-files 'bin/*' | while IFS= read -r f; do
              case "$f" in *.*) continue ;; esac
              head -1 "$f" | grep -qE '^#!.*(bash|sh)$' && printf '%s\n' "$f"
            done
          )
          echo "bin (shebang bash/sh): ${#bin_files[@]} файлов"
          shellcheck -S warning "${bin_files[@]}"
