# Personal runtime data — never commit.
projects.yaml
*.log
*.err
*.out

# Local working TODO lists — kept in docs/dev/ but not versioned.
docs/dev/todo-*.md

# OS / editor noise
.DS_Store
*.swp

# Python bytecode (адаптеры channels/* импортируются как модули при парити-проверках)
__pycache__/
*.pyc
